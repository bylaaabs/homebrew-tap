# service.yaml - what this repo is. must agree with map/map/services.yaml; ci runs
# `node map/scripts/check.mjs service.yaml`. schema and examples: map/schema/service.yaml.
name: homebrew-tap
hostname: null
kind: data
language: ruby
build: none
deploy: none - brew tap bylaaabs/tap reads main
runs_on: null
owner: product
